"""
This project has been converted to a backend API using FastAPI.

Use `uvicorn app:app --reload --host 0.0.0.0 --port 8000` to run the server.

Endpoints:
 - POST /api/v1/ats/evaluate
 - POST /api/v1/ats/improve
 - POST /api/v1/ats/match

See `app.py` for implementation.
"""

if __name__ == "__main__":
    print("This project is now an API server. Run: uvicorn app:app --reload --host 0.0.0.0 --port 8000")
